apiVersion: v2
name: microservice
description: FINCH 마이크로서비스 공용 차트 — 모든 서비스가 이 하나를 사용한다
type: application

# 차트 자체의 버전. 템플릿을 고칠 때 올린다.
version: 0.3.0

# 앱 버전은 서비스별 values.yaml의 image.tag가 결정하므로 의미 없다.
appVersion: "0.1.0"

maintainers:
  - name: FINCH Infra
    url: https://github.com/tpals0409/finch-gitops
